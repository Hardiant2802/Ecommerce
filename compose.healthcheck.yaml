x-healthcheck-defaults: &healthcheck-defaults
  interval: 5s
  timeout: 5s
  retries: 6

x-healthcheck-slow-start: &healthcheck-slow-start
  <<: *healthcheck-defaults
  start_period: 60s

services:
  app:
    healthcheck:
      test: "curl --fail 127.0.0.1:8000"
      <<: *healthcheck-defaults
    depends_on:
      frontend:
        condition: service_healthy
      phpfpm:
        condition: service_started

  frontend:
    healthcheck:
      test: "wget --no-verbose --tries=1 --spider 127.0.0.1:3000 || exit 1"
      <<: *healthcheck-slow-start

  phpfpm:
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      opensearch:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      #elasticsearch:
      #  condition: service_healthy

  db:
    healthcheck:
      test: "mysqladmin ping -h localhost -u root -pmagento || mariadb-admin ping -h localhost -u root -pmagento"
      <<: *healthcheck-defaults

  redis:
    healthcheck:
      test: "redis-cli ROLE | head -n 1 | grep -q '^master$' || exit 1"
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 60s

  opensearch:
    healthcheck:
      test: "curl --fail opensearch:9200/_cat/health >/dev/null || exit 1"
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 60s

  #elasticsearch:
  #  healthcheck:
  #    test: "curl --fail elasticsearch:9200/_cat/health >/dev/null || exit 1"
  #    <<: *healthcheck-slow-start

  rabbitmq:
    healthcheck:
      test: "rabbitmq-diagnostics -q ping"
      <<: *healthcheck-slow-start

  mailcatcher:
    healthcheck:
      test: "wget --no-verbose --tries=1 --spider 127.0.0.1:1080 || exit 1"
      <<: *healthcheck-defaults
